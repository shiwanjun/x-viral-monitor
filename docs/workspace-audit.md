# X-Tools 数据中心功能审计

审计日期：2026-08-13

## 完成情况

- 官网 `/workspace` 已作为登录后统一数据中心，读取 Better Auth 用户信息和订阅状态；无扩展时仍显示登录账号并给出连接指引。
- 扩展已用 IndexedDB 隔离 X 账号并保存书签、点赞、本人原创推文、本人回复、标签、文件夹、同步状态、软删除和云端变更队列。
- 自动增量同步每 30 分钟最多触发一次、单类最多 5 页并在遇到高水位后停止；完整同步支持暂停、游标续传、2.6 秒节流与 429 指数退避。
- 表格、画廊、统计、搜索、类型/媒体/日期/标签/文件夹筛选、保存智能筛选、单行菜单、批量整理、30 天归档恢复均已闭环。
- Free 最近 1,000 条和 Pro 100,000 条额度、跨页 JSON/Markdown/CSV/PDF/媒体清单导出、X 写操作最多 50 条串行执行及目标校验均已实现。
- AI 分类支持 X Grok、OpenAI 兼容接口和 Ollama；密钥仅保存在扩展本地，不上传服务端。
- 关系和取关历史已进入同一数据中心；关注率统一按对方关注数除以粉丝数计算，取关历史和关系云同步受 Pro 门控。
- 云备份已实现 D1 清单、R2 gzip 变更块、单账号绑定、幂等批次、游标冲突恢复、只读保留和到期 30 天定时清理。
- 正式环境数据中心开关已开启，生产 D1 表和 R2 绑定已配置；`https://x.jieyiai.dev/workspace` 已返回新版页面。
- 高保真设计稿与正式页面 JS、扩展样式文件保持同步，设计资产记录在 `designs/x-tools-data-center/_d_meta.json`。

## 自动化与构建证据

- `npm test`：31 个测试文件，392 项全部通过。
- 覆盖 10 万条 IndexedDB 写入与分页、账号隔离、额度、冲突合并、高水位、续传、来源软删除、AI Grok 桥、关系数据和官网登录工作台。
- 关注雷达浏览器回归覆盖：时间线数值更新、用户列表批量关注率、胶囊与 X 原按钮不重叠、虚拟列表多轮刷新不重复。
- `npm run build:dist` 已生成 Store 渠道解压目录；源文件与 `dist/` 字节级一致。
- `worker` TypeScript 检查通过；生产路由与远程 D1 的四张数据中心表已验证，当前生产 Worker 版本为 `9ee1324b-5617-4b32-be84-8412687abf93`。

## 仍需账号侧最终确认

- Chrome 控制通道本轮未连接，无法代替用户在真实登录 X 页面点击最终写操作；重新加载 `dist/` 后需在实际时间线、关注/粉丝列表各滚动一轮确认 DOM 未被 X 临时灰度改版。
- 取消书签、取消点赞、删除本人推文属于真实外部写操作，只有用户明确选中并二次确认后才应执行；自动化已验证请求契约、串行限制和本地状态更新，不会在验收中擅自删除数据。
- 两台真实设备的跨设备恢复需要两个已登录浏览器环境；本地冲突与远端 API 契约已自动化覆盖。

## 当前加载方式

- Chrome 扩展页点击“重新加载”，或重新加载解压目录：`/Users/summer/project/x-viral-monitor/dist`。
- 本地工作台：`http://127.0.0.1:8877/workspace.html?v=7`。
- 正式工作台：`https://x.jieyiai.dev/workspace`。
